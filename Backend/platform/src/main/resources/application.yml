spring:
  datasource:
    url: jdbc:mysql://mmidb.cjkoicgcec3k.ap-southeast-2.rds.amazonaws.com:3306/mmi_db?useSSL=true&requireSSL=true
    username: admin
    password: mmidbpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080